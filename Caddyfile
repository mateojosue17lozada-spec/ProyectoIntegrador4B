optica-integral.duckdns.org {
    # Proxy para las peticiones API del backend
    handle /api/* {
        reverse_proxy backend:3000
    }

    # Proxy para el frontend web Nginx
    handle {
        reverse_proxy frontend:80
    }
}
